          # Usando Counter para contar as ocorrências de cada letra
        contagem_letras = Counter(lista_letras)

        # Exibindo o resultado
        for letra, contagem in contagem_letras.items():
            p